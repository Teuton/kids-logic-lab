import { gcd,getFactors } from '../math/core.js';import { renderShell } from '../ui/shell.js';import { toast,playTone } from '../ui/components.js';import { completeLevel } from './common.js';
export function renderCoprimeGate(root,ctx){const pairs=[[7,11],[7,21],[8,15],[9,15],[14,25]];let i=0,wrong=0,hints=0;const screen=renderShell(root,ctx.state,{title:'🏰 互質守門員',back:()=>ctx.navigate('#map'),content:`<section class="hero-card gate-card"><div class="npc">🛡️</div><div><h1>GCD = 1 才能通過</h1><p>兩個「不同的」質數一定互質，但互質不代表兩個數都要是質數。</p></div></section><section class="play-card"><div class="castle-gate" data-gate><div class="gate-sign">GCD = 1</div><div class="pair-orb" data-pair></div></div><div class="choice-row"><button class="btn btn-primary" data-pass>🚪 開門</button><button class="btn btn-secondary" data-block>🛑 擋下</button></div><div class="hint-row"><span data-feedback>先算兩數的最大公因數。</span><button class="btn btn-hint" data-hint>💡 提示</button></div></section>`});const pairNode=screen.querySelector('[data-pair]'),feedback=screen.querySelector('[data-feedback]');function show(){const[a,b]=pairs[i];pairNode.textContent=`${a} ＆ ${b}`;feedback.textContent=`第 ${i+1}/${pairs.length} 組：能通過嗎？`;}function answer(pass){const[a,b]=pairs[i],g=gcd(a,b),expected=g===1;if(pass===expected){playTone('correct',ctx.state.settings.sound);screen.querySelector('[data-gate]').classList.add(expected?'gate-open':'gate-block');toast(expected?'互質！城門打開':'抓到了共同因數！','success');setTimeout(()=>{screen.querySelector('[data-gate]').classList.remove('gate-open','gate-block');i++;if(i===pairs.length)completeLevel({state:ctx.state,levelId:'coprime-gate',accuracy:Math.max(.6,1-wrong*.1),hintsUsed:hints,anchor:pairNode,navigate:ctx.navigate});else show();},500);}else{wrong++;playTone('wrong',ctx.state.settings.sound);const common=getFactors(a).filter(n=>getFactors(b).includes(n));feedback.textContent=`再想想。${g>1?`共同因數有 ${common.join('、')}`:'這兩數的最大公因數其實是 1。'}`;}}screen.querySelector('[data-pass]').addEventListener('click',()=>answer(true));screen.querySelector('[data-block]').addEventListener('click',()=>answer(false));screen.querySelector('[data-hint]').addEventListener('click',()=>{hints++;const[a,b]=pairs[i];feedback.textContent=`提示：GCD(${a}, ${b}) = ${gcd(a,b)}。`;});show();}
