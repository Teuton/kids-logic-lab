import {renderShell} from '../ui/shell.js';import {playTone} from '../ui/components.js';import {candyFilterCount} from '../math/advanced.js';import {completeLevel} from './common.js';
const make=()=>Array.from({length:96},(_,i)=>({id:i+1,color:['red','green','yellow','blue'][i%4],shape:i%16<4?'star':'round'}));
export function renderCandyFilter(root,ctx){const all=make();let red=false,blueStar=false,hints=0,checks=0;const s=renderShell(root,ctx.state,{title:'🍬 96→66 條件篩選器',back:()=>ctx.navigate('#map'),content:`<section class="hero-card"><div class="npc">🍬</div><div><h1>條件篩選器</h1><p>96 顆糖果，排除紅色，再排除藍色星星。</p></div></section><section class="play-card"><div class="filter-controls"><label><input type="checkbox" data-red> 排除紅色</label><label><input type="checkbox" data-blue> 排除藍色星星</label></div><div class="filter-equation">96 − <b data-r>0</b> − <b data-b>0</b> = <strong data-left>96</strong></div><div class="candy-grid" data-grid></div><div class="hint-row"><span data-feedback>一次開一個條件觀察。</span><button class="btn btn-hint" data-hint>💡 提示</button></div><div class="answer-row"><label>最後剩下：<input type="number" data-answer></label><button class="btn btn-primary" data-check>檢查</button></div></section>`});
const predicates=()=>[...(red?[c=>c.color!=='red']:[]),...(blueStar?[c=>!(c.color==='blue'&&c.shape==='star')]:[])];function draw(){const visible=all.filter(c=>predicates().every(f=>f(c)));s.querySelector('[data-r]').textContent=red?24:0;s.querySelector('[data-b]').textContent=blueStar?6:0;s.querySelector('[data-left]').textContent=visible.length;s.querySelector('[data-grid]').innerHTML=all.map(c=>`<span class="candy ${c.color} ${c.shape} ${visible.includes(c)?'':'filtered'}">${c.shape==='star'?'★':'●'}</span>`).join('');}
s.querySelector('[data-red]').onchange=e=>{red=e.target.checked;draw();};s.querySelector('[data-blue]').onchange=e=>{blueStar=e.target.checked;draw();};s.querySelector('[data-hint]').onclick=()=>{hints++;s.querySelector('[data-feedback]').textContent=hints===1?'紅色是 96÷4=24 顆。':hints===2?'藍色星星共有 6 顆。':'96−24−6=66。';};s.querySelector('[data-check]').onclick=e=>{checks++;const n=candyFilterCount(all,[c=>c.color!=='red',c=>!(c.color==='blue'&&c.shape==='star')]);if(red&&blueStar&&Number(s.querySelector('[data-answer]').value)===n)completeLevel({state:ctx.state,levelId:'candy-filter',accuracy:Math.max(.6,1-(checks-1)*.12),hintsUsed:hints,anchor:e.currentTarget,navigate:ctx.navigate});else{s.querySelector('[data-feedback]').textContent='兩個條件都要開啟，再算剩下數量。';playTone('wrong',ctx.state.settings.sound);}};draw();}
