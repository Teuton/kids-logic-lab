import { gcdMany } from '../math/core.js';import { renderShell } from '../ui/shell.js';import { completeLevel } from './common.js';import { playTone } from '../ui/components.js';
export function renderLampMission(root,ctx){const sides=[120,168,192],answer=gcdMany(sides),choices=[6,8,12,24,48];let selected=0,wrong=0,hints=0;const screen=renderShell(root,ctx.state,{title:'💡 BOSS：路燈工程',back:()=>ctx.navigate('#map'),content:`<section class="hero-card lamp-card"><div class="npc">👷</div><div><h1>燈越少，間距要越大</h1><p>三角形三邊 120m、168m、192m。每盞燈距離要完全一樣，而且三個頂點都要有燈。</p></div></section><section class="play-card"><div class="triangle-park"><div class="park-side side-a">120m</div><div class="park-side side-b">168m</div><div class="park-side side-c">192m</div><div class="park-center">🌳 公園</div></div><h3>選擇路燈間距</h3><div class="choice-row">${choices.map(n=>`<button class="choice-btn" data-distance="${n}">${n}m</button>`).join('')}</div><div class="lamp-check" data-result>選一個間距，工程圖會檢查三條邊。</div><div class="edge-status" data-edges></div><div class="hint-row"><span data-feedback>一起切 → 找最大的共同間距。</span><button class="btn btn-hint" data-hint>💡 提示</button></div><button class="btn btn-primary" data-submit disabled>送出工程方案</button></section>`});screen.querySelectorAll('[data-distance]').forEach(btn=>btn.onclick=()=>{selected=Number(btn.dataset.distance);screen.querySelectorAll('[data-distance]').forEach(b=>b.classList.toggle('selected',b===btn));const statuses=sides.map(side=>({side,ok:side%selected===0,count:side/selected}));screen.querySelector('[data-edges]').innerHTML=statuses.map(s=>`<span class="edge-pill ${s.ok?'ok':'bad'}">${s.side}m ${s.ok?`✓ ${s.count} 段`:'✕ 不能整除'}</span>`).join('');const all=statuses.every(s=>s.ok);screen.querySelector('[data-result]').textContent=all?`三邊都可完整排列。周長 480m，共 ${480/selected} 盞。`:'至少一條邊無法等距排完。';screen.querySelector('[data-submit]').disabled=!all;playTone(all?'correct':'wrong',ctx.state.settings.sound);});screen.querySelector('[data-hint]').onclick=()=>{hints++;screen.querySelector('[data-feedback]').textContent=hints===1?'三條邊的燈距必須完全一樣。':hints===2?'希望燈最少，所以合法的間距要盡量大。':'找能同時整除 120、168、192 的最大數。';};screen.querySelector('[data-submit]').onclick=e=>{if(selected===answer)completeLevel({state:ctx.state,levelId:'lamp-mission',accuracy:Math.max(.6,1-wrong*.1),hintsUsed:hints,anchor:e.currentTarget,navigate:ctx.navigate});else{wrong++;screen.querySelector('[data-feedback]').textContent=`${selected}m 雖然可行，但還不是最大的共同間距。再試更大的合法值。`;}};}
