import { lcm } from '../math/core.js';import { renderShell } from '../ui/shell.js';import { completeLevel } from './common.js';
export function renderRunningTrack(root,ctx){const lapA=150,lapB=120,meet=lcm(lapA,lapB);let elapsed=0,running=false,speed=1,timer=null,meetings=0,hints=0;const screen=renderShell(root,ctx.state,{title:'🏃 BOSS：操場追逐戰',back:()=>ctx.navigate('#map'),content:`<section class="hero-card track-card"><div class="npc">🏃‍♀️🏃</div><div><h1>6:00 同時出發</h1><p>小南 150 秒一圈，老師 120 秒一圈。找出再次同時經過起點的時間。</p></div></section><section class="play-card"><div class="clock" data-clock>6:00:00</div><div class="oval-track"><div class="track-runner runner-a" data-a>🧒</div><div class="track-runner runner-b" data-b>🧑‍🏫</div><span class="start-line">起點</span></div><div class="meeting-banner" data-banner>共同起點次數：0</div><div class="control-row"><button class="btn btn-primary" data-start>▶ 開始</button><button class="btn btn-secondary" data-pause>⏸ 暫停</button><button class="btn btn-secondary" data-speed>1×</button><button class="btn btn-secondary" data-reset>↺ 重來</button></div><div class="hint-row"><span data-feedback>「再次同時」是在找兩個週期的最小公倍數。</span><button class="btn btn-hint" data-hint>💡 提示</button></div><div class="answer-bar"><label>每隔幾分鐘再次一起回起點？<input data-answer inputmode="numeric" type="number"></label><button class="btn btn-primary" data-check>確認</button></div></section>`});function draw(){const hour=6+Math.floor(elapsed/3600),min=Math.floor((elapsed%3600)/60),sec=elapsed%60;screen.querySelector('[data-clock]').textContent=`${hour}:${String(min).padStart(2,'0')}:${String(sec).padStart(2,'0')}`;const a=(elapsed%lapA)/lapA*360,b=(elapsed%lapB)/lapB*360;screen.querySelector('[data-a]').style.transform=`rotate(${a}deg) translateX(112px) rotate(${-a}deg)`;screen.querySelector('[data-b]').style.transform=`rotate(${b}deg) translateX(82px) rotate(${-b}deg)`;}function step(){if(!running)return;const prev=elapsed;elapsed=Math.min(3600,elapsed+speed*10);if(Math.floor(prev/meet)<Math.floor(elapsed/meet)&&elapsed>0){meetings++;const b=screen.querySelector('[data-banner]');b.textContent=`✨ 同時回起點！第 ${meetings} 次`;b.classList.add('flash');setTimeout(()=>b.classList.remove('flash'),500);}else screen.querySelector('[data-banner]').textContent=`共同起點次數：${meetings}`;draw();if(elapsed>=3600){running=false;clearInterval(timer);}}screen.querySelector('[data-start]').onclick=()=>{if(elapsed>=3600){elapsed=0;meetings=0;}running=true;clearInterval(timer);timer=setInterval(step,80);};screen.querySelector('[data-pause]').onclick=()=>{running=false;clearInterval(timer);};screen.querySelector('[data-speed]').onclick=e=>{speed=speed===1?2:speed===2?4:1;e.currentTarget.textContent=`${speed}×`;};screen.querySelector('[data-reset]').onclick=()=>{running=false;clearInterval(timer);elapsed=0;meetings=0;draw();screen.querySelector('[data-banner]').textContent='共同起點次數：0';};screen.querySelector('[data-hint]').onclick=()=>{hints++;screen.querySelector('[data-feedback]').textContent=hints===1?'150 秒和 120 秒多久會同時整除？':hints===2?'LCM(150,120)=600 秒。':'600 秒 = 10 分鐘；6:00～7:00 不算起跑，共 6 次。';};screen.querySelector('[data-check]').onclick=e=>{if(Number(screen.querySelector('[data-answer]').value)===10)completeLevel({state:ctx.state,levelId:'running-track',accuracy:1,hintsUsed:hints,anchor:e.currentTarget,navigate:ctx.navigate});else screen.querySelector('[data-feedback]').textContent='把秒數換成分鐘再試一次。';};draw();return()=>clearInterval(timer);}
