import { isPrime } from '../math/core.js';import { renderShell } from '../ui/shell.js';import { createDragManager,toast,playTone } from '../ui/components.js';import { completeLevel,hintButton } from './common.js';import { questions } from '../data/questions.js';
export function renderPrimeForest(root,ctx){const nums=[17,27,37,47,578],q=questions.find(x=>x.id==='prime-count-001');let hints=0,attempts=0,done=0;const screen=renderShell(root,ctx.state,{title:'🌲 質數森林',back:()=>ctx.navigate('#map'),content:`<section class="hero-card forest-card"><div class="npc">🦊</div><div><h1>數字迷路了！</h1><p>把每張卡拖到正確的箱子。質數只有 1 和自己兩個正因數。</p></div></section><section class="play-card"><div class="number-tray">${nums.map(n=>`<button class="number-card draggable" data-number="${n}">${n}</button>`).join('')}</div><div class="sort-bins"><div class="drop-zone prime-bin" data-kind="prime"><b>✨ 質數箱</b><small>只有 1 和自己</small></div><div class="drop-zone composite-bin" data-kind="composite"><b>🧱 合數箱</b><small>還有別的因數</small></div></div><div class="hint-row"><span data-hint-panel>拖一張卡試試看。</span></div><div data-actions></div></section>`});const actions=screen.querySelector('[data-actions]');actions.append(hintButton(q,u=>hints=u));createDragManager({cards:screen.querySelectorAll('.number-card'),targets:screen.querySelectorAll('.drop-zone'),onDrop(card,target){attempts++;const n=Number(card.dataset.number),expected=isPrime(n)?'prime':'composite';if(target.dataset.kind===expected){card.dataset.locked='true';card.classList.add('solved');target.append(card);done++;playTone('correct',ctx.state.settings.sound);toast(`${n} 放對了！ +XP`,'success');if(done===nums.length)askCount();}else{playTone('wrong',ctx.state.settings.sound);card.classList.add('shake');setTimeout(()=>card.classList.remove('shake'),450);screen.querySelector('[data-hint-panel]').textContent=`${n} 再想想：除了 1 和自己，還能不能被其他數整除？`;}}});function askCount(){actions.innerHTML=`<div class="mini-challenge"><b>最後一題：這五個數有幾個質數？</b><div class="choice-row">${[2,3,4,5].map(n=>`<button class="choice-btn" data-count="${n}">${n}</button>`).join('')}</div></div>`;actions.querySelectorAll('[data-count]').forEach(b=>b.addEventListener('click',()=>{attempts++;if(Number(b.dataset.count)===3)completeLevel({state:ctx.state,levelId:'prime-forest',accuracy:Math.max(.6,6/Math.max(6,attempts)),hintsUsed:hints,anchor:b,navigate:ctx.navigate});else{playTone('wrong',ctx.state.settings.sound);toast('再數一次亮著的質數卡！','error');}}));}}
